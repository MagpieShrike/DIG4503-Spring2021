import React from 'react';

// Fuction Component

const MarketItem = (props) => {

    return (
        <div>
            <p> Item {props.count}</p>
        </div>
    );
}

export default MarketItem;